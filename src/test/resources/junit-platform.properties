# Classes run concurrently, methods within a class run sequentially.
# Deliberate: most of the wall-clock win, intra-class ordering stays trivially safe,
# and concurrent load on the public test services is capped at 4 - the assignment
# explicitly asks us not to overload them.
junit.jupiter.execution.parallel.enabled = true
junit.jupiter.execution.parallel.mode.default = same_thread
junit.jupiter.execution.parallel.mode.classes.default = concurrent
junit.jupiter.execution.parallel.config.strategy = fixed
junit.jupiter.execution.parallel.config.fixed.parallelism = 4

# Readable names in reports without repeating the method name in @DisplayName.
junit.jupiter.displayname.generator.default = org.junit.jupiter.api.DisplayNameGenerator$ReplaceUnderscores